plugins {
    id "kotlin"
    id "application"
    id "com.github.johnrengelman.shadow" version "5.1.0"
}

dependencies {
    implementation(project(":common"))

    // Crawling
    implementation("org.jsoup:jsoup:1.12.1")
}

application {
    mainClassName = "de.hpi.cloud.news.crawler.MainKt"
}
